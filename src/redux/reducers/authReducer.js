const DEFAULT_STATE = {
    token: false,
};

const authReducer = (state = DEFAULT_STATE, action) => {
    switch (action.type) {
        default: {
            return state;
        }
    }
};

export default authReducer;
